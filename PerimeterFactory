public class PerimeterFactory implements BaseFactory{
    public Circle createCircle(int r) {
        return new PerimeterCircle(r);
    }

    public Triangle createTriangle(int a, int b, int c){
        return new PerimeterTriangle(a, b, c);
    }
}
